{% extends '::layout.html.twig' %}

{% block title -%} Maschine creation {% endblock %}
{% block content -%}
    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a class="btn btn-default" href="{{ path('maschine') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
