{% extends '::layout.html.twig' %}

{% block title %}Benutzer '{{user.username}}' bearbeiten{% endblock %}

{% block content %}
	<div class="container">
		<div class="row">
			<div class="col col-md-6">
				{{form(form)}}
			</div>
			<div class="col col-md-6">
				<h3>Zugriffsrechte</h3>
				<table class="table table-condensed">
				{% for key,granted in resultingfeatures %}
					<tr>
						<td>{{key}}</td>
						<td>
							{% if granted %}
								<span class="label label-success">ja</span>
							{% else %}
								<span class="label label-danger">nein</span>
							{% endif %}
						</td>
					</tr>
				{% endfor %}
				</table>
			</div>
		</div>
	</div>
    <div class="btn-group" role="group">
    <a class="btn btn-default"
       href="{{ path('admin_index') }}">
        Zurück zur Liste
    </a>
	</div>
{% endblock %}
