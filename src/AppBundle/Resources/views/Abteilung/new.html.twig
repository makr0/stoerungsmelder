{% extends '::layout.html.twig' %}

{% block title -%} Abteilung creation {% endblock %}
{% block content -%}
    {{ form(form) }}

        <div class="btn-group" role="group">
    <a class="btn btn-default" href="{{ path('abteilung') }}">
        Back to the list
    </a>
</div>
{% endblock %}
