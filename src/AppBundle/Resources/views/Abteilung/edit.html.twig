{% extends '::layout.html.twig' %}

{% block title -%} Abteilung bearbeiten {% endblock %}
{% block content -%}

    {{ form(edit_form) }}

        <div class="btn-group" role="group">
    <a class="btn btn-default" href="{{ path('abteilung') }}">
        Zurück zur Liste
    </a>
    {{ form(delete_form) }}
</div>
{% endblock %}
