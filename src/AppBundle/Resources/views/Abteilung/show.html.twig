{% extends '::layout.html.twig' %}

{% block title -%} Abteilung {% endblock %}
{% block content -%}
    <table class="table table-striped">
        <tbody>
            <tr>
                 <td class="field-label col-xs-2 active">
                    <label>Id</label>
                </td>
                <td class="col-md-10">
                    {{ entity.id }}
                </td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

        <div class="btn-group" role="group">
    <a class="btn btn-default" href="{{ path('abteilung') }}">
        Zurück zur Liste
    </a>
        <a class="btn btn-default" href="{{ path('abteilung_edit', { 'id': entity.id }) }}">
            Bearbeiten
        </a>
    {{ form(delete_form) }}
</div>
{% endblock %}
