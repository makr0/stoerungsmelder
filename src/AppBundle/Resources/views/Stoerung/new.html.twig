{% extends '::layout.html.twig' %}

{% block title -%} Stoerung anlegen {% endblock %}
{% block content -%}
    {{ form(form) }}

        <div class="btn-group" role="group">
    <a class="btn btn-default" href="{{ path('stoerung') }}">
        Zurück zur Liste
    </a>
</div>
{% endblock %}
